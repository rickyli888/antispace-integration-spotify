import type { AntispaceMetadata } from "@antispace/sdk";
export * from "./spotify";

export const setNickname = async (
	nickname: string,
	anti: AntispaceMetadata,
) => {
	// placeholder
};

export const sayNickname = async (anti: AntispaceMetadata) => {
	// placeholder
};
